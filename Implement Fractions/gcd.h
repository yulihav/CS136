// This module provides functions for computing the gcd and lcm of two numbers

// gcd(a, b) returns the gcd of a and b
int gcd(const int a, const int b);

// lcm(a, b) returns the lcm of a and b
int lcm(const int a, const int b);

